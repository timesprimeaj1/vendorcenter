# Leave empty for local dev (uses Vite proxy). Set to backend URL for production.
# Local: empty (Vite proxies /api to localhost:4000)
# Production (Vercel): https://your-railway-backend-url.up.railway.app
VITE_API_BASE_URL=
