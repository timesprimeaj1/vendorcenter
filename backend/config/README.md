# backend/config

Reserved for backend-level configuration artifacts and templates.

Current runtime env parsing remains in:
- backend/src/config/env.ts

This folder is added to match the company-grade repository layout.
